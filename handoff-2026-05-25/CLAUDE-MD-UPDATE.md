# Обновление для CLAUDE.md в репозитории

> Применить через Claude Code: добавить эти секции в файл `CLAUDE.md` корня проекта `u1u2-bridge`.

---

## Изменение 1 — в раздел «Architecture», добавить новую подсекцию

Добавить **после блока «Сеть — UDP и WireGuard»** (или в любом логичном месте раздела архитектуры):

```markdown
### Архитектурный roadmap для подключения пульта

В проекте два независимых варианта получения CRSF от пульта на u1-pi. Один — текущий, второй — запланированный план B.

**Пульт №1 — CRSF через JR-bay (текущая итерация).** Boxer ELRS подключается через переходник JR-bay → SN74HC14N (Schmitt inverter) → CH340G USB-UART → u1-pi. Прозрачный байтовый bridge UART↔UDP. Работают Lua-скрипты и TX menu пульта. Латенция ~2–4 мс. Требует пайки и аккуратности с pin 3 VBAT (7.8 В).

**Пульт-джойстик №2 — USB HID (отложенный план B, обязательно применим позже).** Boxer (или любой USB-пульт/джойстик/gamepad) подключается напрямую USB-C к u1-pi как HID-устройство. На u1 — `joystick_to_udp.py` читает evdev и пакует 16 каналов в UDP. На u2 — `joystick_to_crsf.py` генерирует CRSF frames (sync 0xC8, type 0x16, channel data, CRC8) и подаёт на ELRS через UART7. Латенция +10 мс vs варианта №1 (USB-HID poll 125–1000 Hz). Не работают Lua-скрипты, нет telemetry обратно. **Главный плюс — универсальность пульта**: любой USB-gamepad может заменить Boxer в поле, нулевая пайка, нет риска спалить UART.

Решение по варианту №2: применять обязательно, но после того как вариант №1 докажет работоспособность инфраструктуры. На момент написания этой записи — работаем по варианту №1.
```

---

## Изменение 2 — в раздел «Lessons & Incidents», добавить две новые записи

Добавить **сверху** (выше записи от 2026-05-13):

```markdown
### 2026-05-25 · Цвета линий на тёмной теме сливаются с фоном

В диаграммах распайки переходника Boxer→u1 GND-линии `#444441` на фоне `#1a1a18` визуально сливались — разница `#2A` контраста недостаточна для тёмной темы Edge. Несколько итераций SVG было потрачено на подбор палитры пока не дошло.

**Правило:** для нейтральных линий (GND, leader lines) использовать gray-400 (`#888780`) mid-tone — видимо в обоих режимах. Для семантических сигналов — mid-tone из палитры (green-400/500 `#3B6D11`/`#639922`, coral-400 `#D85A30`, blue-400/600 `#185FA5`/`#378ADD`). Фон в standalone SVG задаётся явным `<rect fill="#1a1a18">` для корректного открытия в Edge с force-dark.

**Проверка:** мысленный тест — каждый элемент должен быть виден на полностью чёрном фоне. Если разница яркости элемента и фона < `#40` — переделать.

### 2026-05-25 · Pinout JR-bay Boxer ELRS — стандартный, но pin 3 опасен

При подготовке к распайке переходника пульт→u1 измерили JR-bay мультиметром: p1=3.3 В (PPM idle high), p2=0 В (не разведён), **p3=7.8 В (VBAT, спалит UART)**, p4=0 В (GND), p5=0.27 В (S.PORT idle low). Pinout стандартный, ничего нестандартного у Boxer ELRS нет.

**Правило:** при работе с любыми JR-bay коннекторами (даже стандартными) — обязательно проверять мультиметром каждый pin **до** подключения к Pi/UART. Pin 3 VBAT на разных пультах может быть 6–8.4 В — гарантированно сжигает 3.3 В UART, если перепутать.

**Проверка:** перед подключением — мультиметр в режим напряжения, измерить каждый pin относительно pin 4 (GND) с включённым пультом. Сверить с datasheet модели.
```

---

## Изменение 3 — в самый верх файла, обновить раздел «Project Context»

Заменить строку про пульт:

```markdown
- **Продукт:** беспроводная замена 8-жильного кабеля между двумя устройствами FPV-наземки (У1 — мастер-пульт с TX12 + видео-передатчиком к очкам, ...)
```

на:

```markdown
- **Продукт:** беспроводная замена 8-жильного кабеля между двумя устройствами FPV-наземки (У1 — мастер-пульт с **RadioMaster Boxer ELRS** (заменил TX12) + видео-передатчиком к очкам, ...)
```

---

## Изменение 4 — в раздел «Commands → На Orange Pi», добавить строку

После `Статус CRSF-моста` добавить:

```markdown
| Логи CRSF P1 (от пульта) | `journalctl -u crsf-bridge@p1 -f` |
```

---

*Эти 4 изменения — минимально необходимые для отражения состояния на 25 мая 2026. Применить через Claude Code одним коммитом с сообщением:*

```
docs: записать Пульт-джойстик №2 в roadmap, обновить lessons и контекст

- Добавлен архитектурный roadmap с двумя вариантами подключения пульта:
  - Пульт №1 — CRSF через JR-bay (текущая итерация)
  - Пульт-джойстик №2 — USB HID (отложен, применим позже)
- Lessons: пайка GND-линий в SVG для тёмной темы
- Lessons: pinout JR-bay Boxer ELRS подтверждён мультиметром
- Context: TX12 заменён на Boxer ELRS
- Commands: добавлена команда для логов crsf-bridge@p1
```
