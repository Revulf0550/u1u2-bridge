# HANDOFF → Этап B (B2) · u1u2-bridge · 2026-06-04

> Продолжение HANDOFF-2026-06-04-stageB-cont.md. B1/B-telem/B3 закрыты. Осталось B2.
> §0: любая работа — после сверки с фактом (репо + железо), не по памяти.

## СНАЧАЛА ПРОЧИТАЙ
- `docs/PLAN.md` — мастер-план. Этап A ✅, B1 ✅, B-telem → backlog, **B3 ✅**. Осталось **B2**.
- `docs/handoff/HANDOFF-2026-06-04-stageB-cont.md` — предыдущая точка входа (B1/B-telem). Этот файл — её продолжение.
- `docs/baseline/` — точка отката CRSF-конфига. НЕ редактировать.
- `CLAUDE.md` — Lessons & Incidents + Architecture. Долг: trim (59k > 40k) отдельной сессией, НЕ в B.

## ВЫПОЛНЕНО (сессия 2026-06-04, продолжение)
- **§0-аудит** — сверено с историей сессий, входной хэндофф = факту.
- **B3 ✅ — ребут-тест автозапуска пройден на ОБЕИХ Pi** (холодный ребут каждой):
  - u1: `up 1min`; `@p1` + `video-rx` active/enabled/restarts=0; wg enabled/active; `/run/user/0` пересоздан tmpfiles (0700 root) ДО старта video-rx; журнал video-rx — полный конвейер до waylandsink, живой аппаратный декод.
  - u2: `up 1min`; `@elrs` + `video-tx` active/enabled/restarts=0; wg enabled/active; грабер переоткрыт; mpph264enc → RTP шлёт.
  - **Живой RTP подтверждён: 116 пакетов/3с на u1, порт 5600** (не frozen).
  - «Синий не пропал» при ребуте u2 — НЕ баг: waylandsink держит последний кадр, а no-signal тоже синий → freeze невидим. Поток прерывался и оттаял (доказано RTP-замером).
- Коммиты: `45d4f24` (B3 done в PLAN), `4451e72` (handoff stageB-cont). main = origin.
- Косметика: Co-Authored-By прилип к subject (многострочный `-m` склеивается). Оставлено (forward-only). На будущее — трейлер вторым `-m`.

## СОСТОЯНИЕ (факт 2026-06-04)
- **git = `4451e72`** (этот хэндофф может стать +1 коммит, если решишь класть в репо). Незакоммичено: `pyproject.toml` (M, чужое — НЕ трогать); untracked: `docs/2026-05-24_*.png`, `handoff-2026-05-25/`.
- **4 боевых юнита на обеих Pi: active/enabled/restarts=0; автозапуск после ребута ДОКАЗАН.**
- Видео Режим №1 e2e живой (B1) + переживает ребут (B3). CRSF forward жив (~7–33 кБ/с Boxer→ELRS). Телеметрия обратно = 0 (backlog).

## ДАЛЬШЕ — B2 (последнее в Этапе B)
**B2 — CRSF e2e + оценка задержки/потерь.** ⚠️ ПЕРЕОСМЫСЛИТЬ В НАЧАЛЕ СЕССИИ:
классический round-trip НЕЛЬЗЯ (обратный канал = 0). Forward уже наблюдали. Варианты:
- **(а)** one-way / loopback на bench — без дрона, по таймстемпам;
- **(б)** наблюдаемая реакция дрона на стики — **НУЖЕН ДРОН (спросить ЯВНО)**.
Выбор подхода — первым делом, с пользователем. После B2 Этап B закрыт → Этап C (CPE710 + Режим №2), там же полевой замер латенции.

## BACKLOG (docs/PLAN.md «Доработки»)
- Телеметрия на Boxer (back-channel = 0) — после полевых. Гипотеза: не разведён обратный провод ELRS TLM → RX /dev/ttyS7, либо конфиг модуля (half-duplex single-wire). НЕ код.
- B1-latency glass-to-glass → полевой замер при Этапе C.
- Trim CLAUDE.md.

## ТОПОЛОГИЯ
- **u2 = TX:** `ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.7`. Юниты: `crsf-bridge@elrs` (/dev/ttyS7, аппаратный UART7), `video-tx`.
- **u1 = RX + HDMI-монитор:** `ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.6`. Юниты: `crsf-bridge@p1` (/dev/ttyUSB0, CH340 1a86:7523), `video-rx`.
- CRSF forward: Boxer → u1 ttyUSB0 → `@p1` → UDP 14552 → wg → `@elrs` → u2 ttyS7 → ELRS.
- Транспорт: WireGuard 10.8.0.x через интернет, RTT ~150 мс, iface enP3p49s0. CPE710 НЕ развёрнут (Этап C).
- Деплой: `sudo TRANSPORT=tunnel [SKIP_VIDEO=1] ./install.sh u1|u2` (из ~/u1u2-bridge).

## НЕ ДЕЛАЙ
- НЕ возвращать kmssink (VOP2 timeout). Дисплей — cage+waylandsink.
- НЕ убирать videoconvert перед waylandsink (чёрный экран, NV12).
- НЕ просить у грабера 720×576, НЕ ставить io-mode=4 — Arkmicro только 640×480 MJPG.
- НЕ доверять старому /opt без редеплоя (истина — репо/install.sh).
- `pyproject.toml` — чужое, не трогать. `docs/baseline/` — не редактировать.
- **ДРОН включать — ТОЛЬКО спросив пользователя ЯВНО** (может понадобиться в B2 (б)).
- Многострочный git `-m` склеивается — трейлеры вторым `-m`.
- Большой heredoc через Claude Code дублирует строки при вставке — длинные файлы класть через `present_files`/скачивание, не через вставку в терминал.

## ПРАВИЛА РАБОТЫ
- §0 аудит/план → diff → НЕ коммить без ОК (не «allow all»), поэтапно. Push — после ОК на diff.
- Дозируй: один логический блок, жди подтверждения.
- Помечай «На u1:» / «На u2:» / «В PowerShell:».
- Claude Code установлен — для локальных команд/правок/git; переключайся не молча.
- Монитор на u1. Русский.
