# HANDOFF (дельта) → Репо-уборка завершена · HEAD `40df664` · verify зелёный · дерево чистое · u1u2-bridge · 2026-06-06

> **Точка входа в новый чат.** Это **дельта** поверх базового хэндоффа — не дублирует его.
> **База (истина по железу/сети/сервисам/доступу ARDOR/CPE/свитч Конца A):** `HANDOFF-2026-06-06-mode2-verified-AUDITED.md`.
> **Метки источников ниже:** `[live]` = вывод Claude Code этой сессии; `[base]` = AUDITED-хэндофф (сегодня); `[user]` = подтверждено пользователем в чате.

---

## Обновление к базе

- ✅ **Видео на мониторе u1 — ПОДТВЕРЖДЕНО глазами** `[user]` (в базе стояло «ещё не подтверждали»). Оговорку про no-signal/фриз можно снять.
- Остальное состояние — Режим №2 проверен LIVE, 6 сервисов `active`, CRSF `drop=0`, сеть, свитч на Конце A — **без изменений** `[base]`. Эта сессия **железо/Pi НЕ трогала** (только репо + GitHub).

---

## Что сделала эта сессия — «репо-уборка», всё в `origin/main` `[live]`

HEAD `40df664`. `verify.ps1` зелёный целиком: ruff lint, ruff format (19 файлов), mypy strict (19 файлов), pytest **191 passed**, shellcheck по всем .sh `[live]`. Working tree чистый — untracked-хвостов нет.

- `60cbd08` — **`docs/roadmap/`** с 4 спеками отложенных доработок: `joystick-2-usb-hid.md`, `ctrl-channel.md`, `jitter-latency-tuning.md`, `production-hardening.md`.
- `93597e4` — **архив `handoff-2026-05-25/`** (HANDOFF + CLAUDE-MD-UPDATE + 5 SVG) поставлен под git.
- `fd3e720` — **CLAUDE.md archive-split:** 99.8 → 54.8 КБ; история (32 записи) вынесена в `docs/LESSONS-ARCHIVE.md`; +2 урока (свитч Конца A; «почти все отказы — физика, не софт»); roadmap-секция → указатель на `docs/roadmap/`. Сверка: 62 записи сохранены (30 KEEP + 32 архив), 0 потерь/дублей, текст вербатим.
- `457c201` — **install.sh echo:** печатает режим (direct/tunnel) + transport-aware ping (`${CRSF_PEER%%:*}`); был хардкод «связь по CPE710 (direct)» независимо от режима.
- `24bfc83` — **`pyproject.toml`:** ruff/mypy scope расширен на пакет `u1/` (там живёт Python Step 3.2). Закрыло висевшую с начала сессии незакоммиченную правку (не нашу).
- `40df664` — **`docs/handoff/`:** дозакоммичена серия из 5 handoff'ов (этап C cutover → D → полевые → AUDITED); осиротевший PNG-скриншот удалён. Untracked-хвост закрыт.

---

## Открытых хвостов в репо нет

Working tree чистый, untracked не осталось, всё в `origin/main`. Хвосты `pyproject.toml` (commit `24bfc83`) и `docs/handoff/` (commit `40df664`) закрыты в этой сессии.

---

## Остаётся (меню следующих треков)

1. **Полевые тесты с дроном — главный трек** `[base §СЛЕДУЮЩИЕ ШАГИ]`. PowerShell, **WG-OFF**.
   - (1) Режим №2 + дрон: управление + живое видео на мониторе u1;
   - (2) `switch-mode.ps1 tunnel` → Режим №1 + дрон;
   - (3) power-cycle обеих Pi — что грузится, какой режим по умолчанию (⚠️ u2 без монитора);
   - перетюнить `JITTER_LATENCY` 50 → ~15–20 мс под радио (спека: `docs/roadmap/jitter-latency-tuning.md`).
2. **CLAUDE.md под <40 КБ** (отложено этой сессией) — Claude Code. Сейчас 54.8 КБ; добить = ужать нарратив инцидента в KEEP-записях, оставив **Правило/Проверку**. Деликатно, тем же вербатим-слайсингом по диапазонам строк.
3. **Сборка доработок** когда решишь: joystick-2 (USB-HID) / ctrl-channel — спеки в `docs/roadmap/`.
4. **Трек документации** `[base §ТРЕК ДОКУМЕНТАЦИИ]` — 4 HTML-чит-листа (закоммитить в `docs/`, заполнить заглушки `a-z-instruction`, сверить `switch-mode-cheatsheet` с реальным `switch-mode.ps1`) + нарисовать **SVG u2 заново** (старый рисует u2 устаревше). В этой сессии не трогали.

---

## Старт нового чата

- Открыть чат **внутри проекта** u1u2-bridge (подтянет CLAUDE.md / репо / правила).
- Загрузить: **этот файл** + `HANDOFF-2026-06-06-mode2-verified-AUDITED.md` (база).
- Перед любой правкой железа/конфига — **§0: сверка с живым фактом**, не по памяти (см. урок «хэндофф по памяти» в CLAUDE.md). Полевые тесты — PowerShell при WG-off; репо/git — Claude Code.
