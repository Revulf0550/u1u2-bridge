# HANDOFF → Этап B (продолжение: B2 / B3) · u1u2-bridge · 2026-06-04

> Точка входа в следующую сессию. Составлен ПОСЛЕ выполнения и финальной сверки с фактом
> (git log + git status + is-active/is-enabled/NRestarts всех 4 юнитов на обеих Pi).
> Продолжение HANDOFF-2026-06-04-stageB.md (вход в Этап B). B1 закрыт.

## СНАЧАЛА ПРОЧИТАЙ
- `docs/PLAN.md` — мастер-план (2 режима, этапы A–E). Этап A ✅, **B1 ✅**. В конце добавлена глава **«Доработки» (backlog)**.
- `docs/handoff/HANDOFF-2026-06-04-stageB.md` — предыдущая точка входа (Этап A→B). Этот файл — её продолжение.
- `docs/baseline/` — точка отката рабочей CRSF-конфигурации. НЕ редактировать (tx-имена там намеренные).
- `CLAUDE.md` — Lessons & Incidents (cage-от-root, waylandsink/NV12, грабер Arkmicro) + `## Architecture`.
  Помни: CLAUDE.md ~59k > лимита 40k — есть долг по trim (отдельной сессией, НЕ в B).
- **§0:** любая работа — после сверки с фактом (репо + железо), не по памяти. Хэндофф «по памяти» запрещён.

## ВЫПОЛНЕНО (эта сессия, 2026-06-04)
- **§0-сверка** — Этап A закрыт, 4 юнита зелёные, main синхронизирован (подтверждено фактом).
- **B1 — ✅ done.** Видео e2e Режима №1 с ЖИВЫМ сигналом подтверждено фактом: грэб с грабера показал переход
  ~6.6 КБ (синий no-signal) → 41–93 КБ (реальный кадр с OSD камеры дрона); живая картинка на мониторе u1 через полный
  тракт: грабер Arkmicro (640×480 MJPG) → video-tx (mpph264enc) → RTP/UDP 5600 → wg → video-rx (mppvideodec → cage+waylandsink) → HDMI.
  Зафиксировано в PLAN (коммит `18042be`).
- **B-telem — ПРОВЕРЕНО фактом: обратный поток = 0** с обоих концов (u2 `@elrs` uart->udp=0, u1 `@p1` udp->uart=0;
  прямой RC идёт ~7–33 кБ/с). НЕ-блокер. Перенесено в главу «Доработки» (`63ead57`).
  Важно для понимания: полётные данные (батарея/высота) видны в **OSD по ВИДЕО-каналу** — это отдельно от CRSF и работает;
  «0» лишает только индикации RSSI/LQ канала управления и звуковых предупреждений на пульте. Failsafe на дроне независим.
- **B1-latency — НЕ делали сознательно.** Транспорт сейчас интернет-WireGuard (RTT ~150 мс) → glass-to-glass был бы
  нерепрезентативным. Перенесено на полевой замер при Этапе C (CPE710), тогда же ревизия `JITTER_LATENCY`.
- Коммиты сессии: `18042be` (B1 done), `63ead57` (глава «Доработки» + B-telem в backlog). main = origin.

## СОСТОЯНИЕ (факт 2026-06-04, сверено финальным аудитом)
- **git = `63ead57`**, main...origin/main = **0/0**. Незакоммичено: `pyproject.toml` (M, чужое — НЕ трогать) +
  2 untracked (`docs/2026-05-24_01-17-34.png`, `handoff-2026-05-25/`).
- **4 боевых юнита — active/enabled/NRestarts=0:** u1 `crsf-bridge@p1` + `video-rx`; u2 `crsf-bridge@elrs` + `video-tx`.
  (Не сбились после двойного stop/start `video-tx` в диагностике B1.2.)
- **CRSF Режим №1 (forward) жив:** Boxer → u1 /dev/ttyUSB0 (CH340 1a86:7523) → `crsf-bridge@p1` → UDP 14552 → wg →
  `crsf-bridge@elrs` → u2 /dev/ttyS7 (аппаратный UART7) → ELRS. **Телеметрия (обратно) = 0** — в backlog «Доработки».
- **Видео Режим №1** под systemd, e2e с живой камерой подтверждён.

## ДАЛЬШЕ (Этап B — осталось)
1. **B2 — CRSF e2e + замер задержки.** Forward уже наблюдали (~7–33 кБ/с Boxer→ELRS). Осталось оценить добавленную мостом
   задержку и потери. ⚠️ Классический round-trip замерить нельзя — обратный (телеметрийный) канал = 0. Поэтому B2 надо
   **переосмыслить в начале сессии**: либо one-way задержка по таймстемпам / loopback на bench, либо наблюдаемая реакция
   дрона на стики (нужен дрон — спросить пользователя ЯВНО).
2. **B3 — ребут-тест автозапуска.** Перезагрузить обе Pi → всё поднимается само. Проверить: `@p1`/`@elrs`/`video-*` enabled
   поднялись; `/run/user/0` пересоздан через tmpfiles (иначе video-rx упадёт — известный нюанс cage-от-root).

## ОТЛОЖЕНО / BACKLOG (см. docs/PLAN.md «Доработки»)
- **Телеметрия на Boxer** (CRSF back-channel = 0): RSSI/LQ + предупреждения пульта. Чинить В САМУЮ ПОСЛЕДНЮЮ очередь —
  после закрытия этапов и полевых испытаний. Гипотеза причины: не разведён обратный провод ELRS TLM-выход → RX
  /dev/ttyS7, либо конфиг модуля (half-duplex single-wire CRSF) — НЕ код (бридж симметричен).
- **B1-latency** glass-to-glass → полевой замер при Этапе C (CPE710).
- **Trim CLAUDE.md** (59k > 40k) — отдельной сессией, не в B.

## ТОПОЛОГИЯ
- **u2 = TX:** `ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.7`.
- **u1 = RX + HDMI-монитор:** `ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.6`.
- Транспорт: WireGuard 10.8.0.x через интернет, RTT ~150 мс, iface enP3p49s0. CPE710 НЕ развёрнут (Этап C).
- Деплой: `sudo TRANSPORT=tunnel [SKIP_VIDEO=1] ./install.sh u1|u2` (из ~/u1u2-bridge).

## НЕ ДЕЛАЙ
- НЕ возвращать kmssink (VOP2: wait pd0 off timeout). Дисплей — cage+waylandsink.
- НЕ убирать videoconvert перед waylandsink (чёрный экран, NV12 не согласуется).
- НЕ просить у грабера 720×576 и НЕ ставить io-mode=4 — Arkmicro только 640×480 MJPG.
- НЕ доверять старому /opt без редеплоя (истина — репо/install.sh).
- `pyproject.toml` — чужое незакоммиченное, не трогать. `docs/baseline/` — не редактировать.
- **ДРОН включать — ТОЛЬКО спросив пользователя ЯВНО** (может понадобиться в B2 для реакции на стики).

## ПРАВИЛА РАБОТЫ
- §0 аудит/план → diff → НЕ коммить без ОК (не «allow all»), поэтапно. Push — только после ОК на diff.
- Дозируй: один логический блок, жди подтверждения. Без простыней.
- Помечай блоки «На u1:» / «На u2:» / «В PowerShell:».
- Claude Code установлен — используй для локальных команд / правок / git; переключайся не молча.
- Монитор на u1. Русский.
