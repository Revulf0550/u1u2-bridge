# HANDOFF → Этап B (проверка Режима №1) · u1u2-bridge · 2026-06-04

> Точка входа в следующую сессию. Составлен ПОСЛЕ выполнения и сверки с фактом
> (§0 на 2026-06-04: git log, is-active/is-enabled/NRestarts всех 4 юнитов на обеих Pi, cat docs/PLAN.md).

## СНАЧАЛА ПРОЧИТАЙ
- `docs/PLAN.md` — мастер-план (2 режима, этапы A–E). Этап A помечен ✅ ЗАВЕРШЁН.
- `docs/baseline/` — точка отката рабочей CRSF-конфигурации. НЕ редактировать (tx-имена там намеренные).
- `CLAUDE.md` — Lessons & Incidents (особенно 2026-06-04: cage-от-root, waylandsink/NV12, грабер Arkmicro) + `## Architecture`.
- **§0:** любая работа — после сверки с фактом (репо + железо), не по памяти. Хэндофф «по памяти» запрещён.

## ВЫПОЛНЕНО (Этап A — полностью закрыт)
- **A0/A5/A1/A-clean/A3/A4** — done (прошлая сессия, см. HANDOFF stageA).
- **A2 — видео под systemd (вариант «а»)** — done в этой сессии. `video-rx` (u1) / `video-tx` (u2) заведены сервисами,
  active+enabled, NRestarts=0. cage от root под systemd берёт DRM-master напрямую (libseat-logind фейлится без seat0 →
  builtin backend); минимального юнита достаточно, TTY/PAM-обвязка не понадобилась. E2e через wg подтверждён глазами
  (SMPTE-полосы на мониторе u1). Код A2 уже был вшит в a8573d6 — правок репо не потребовалось.
- **Чистка доков:** tx1/tx2/14550/14551 → p1/elrs/14552 в smoke_test.sh + таблица Commands в CLAUDE.md; udev-секция
  smoke_test переведена на проверку SERIAL_DEV из env (не USB-symlink — u2=ttyS7 аппаратный); коммент install.sh про
  service-mode актуализирован; Lessons-запись про cage; PLAN под «A done».
- Коммиты сессии: b239178 (install-коммент), d0d3dc0 (Lessons cage), e8c9ffc (хвост доков), 20aa254 (smoke serial по env),
  1bdd6d5 (PLAN A done). main синхронизирован с origin.

## СОСТОЯНИЕ (факт 2026-06-04, сверено)
- **git = 1bdd6d5**, main...origin/main = 0/0. Незакоммичено: pyproject.toml (M, чужое — НЕ трогать) + 2 untracked артефакта.
- **4 боевых юнита — active/enabled/NRestarts=0:** u1 `crsf-bridge@p1` + `video-rx`; u2 `crsf-bridge@elrs` + `video-tx`.
- **CRSF Режим №1 жив:** Boxer → u1 /dev/ttyUSB0 (CH340 1a86:7523) → crsf-bridge@p1 → UDP 14552 → wg →
  crsf-bridge@elrs → u2 /dev/ttyS7 (аппаратный UART7) → ELRS.
- **Видео Режим №1 под systemd:** грабер Arkmicro 18ec:5555 (640×480 MJPG @30 — ЕДИНСТВЕННЫЙ режим) → video-tx →
  RTP/UDP 5600 → wg → video-rx (cage+waylandsink, JITTER_LATENCY=50, mtu=1200) → HDMI u1. Живая камера ещё НЕ проверялась (B1).
- **Телеметрия (обратный поток ELRS→пульт) = 0** — ОТКРЫТЫЙ вопрос (B-telem).

## ДАЛЬШЕ (Этап B — проверка Режима №1)
1. **B1 — видео e2e с живой камерой.** Сейчас подтверждены только тест-полосы. Нужна живая картинка с камеры дрона через
   VRX. ВНИМАНИЕ: VRX капризный (по урокам ~4% времени живой кадр), модель неизвестна. **ДРОН включать — ТОЛЬКО на этом
   шаге и ТОЛЬКО спросив пользователя явно.** Монитор на u1.
2. **B2 — CRSF e2e + замер задержки.** Источник байтов — реальный Boxer или синтетика. Замерить round-trip и потери.
3. **B-telem — телеметрия на пульте** (RSSI/LQ, failsafe-предупреждения). ОТКРЫТЫЙ вопрос: одностороннее по дизайну или
   потерян back-channel? СПРОСИТЬ пользователя, чего он ждёт от телеметрии, прежде чем копать.
4. **B3 — ребут-тест автозапуска.** Перезагрузить обе Pi → всё поднимается само. Проверить: @p1/@elrs/video-* enabled
   поднялись; /run/user/0 пересоздан через tmpfiles (иначе video-rx упадёт).

## ОТЛОЖЕНО / ДОЛГ ПО ДОКАМ (отдельной сессией, НЕ в B)
- **Trim CLAUDE.md** — 59k, за лимитом 40k. Отдельный аудит-блок: оставить/сжать/унести-в-архив/выкинуть-дублирующее-код,
  diff перед коммитом, git-история = страховка. Уроки НЕ выкидывать.
- **tx1/tx2 в больших доках** — docs/DEPLOYMENT.md, старый docs/HANDOFF.md, старые docs/handoff/*. Вне scope чистки.
- **Старый docs/HANDOFF.md** — генезис-документ (CP2102N, RS485 «не пришли»), почти весь неактуален. Архивировать или переписать.
- **smoke_test.sh прочие устаревания** — PEER_IP_WG=10.10.0.x (реальность 10.8.0.x), PEER_IP=192.168.1.x (CPE710-дизайн, на железе нет).

## ТОПОЛОГИЯ
- **u2 = TX:** ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.7.
- **u1 = RX + HDMI-монитор:** ssh -i ~/.ssh/u1u2 ubuntu@10.8.0.6.
- Транспорт: WireGuard 10.8.0.x через интернет, RTT ~150 мс, iface enP3p49s0. CPE710 НЕ развёрнут (Этап C).
- Деплой: sudo TRANSPORT=tunnel [SKIP_VIDEO=1] ./install.sh u1|u2 (из ~/u1u2-bridge).

## НЕ ДЕЛАЙ
- НЕ возвращать kmssink (VOP2: wait pd0 off timeout). Дисплей — cage+waylandsink.
- НЕ убирать videoconvert перед waylandsink (чёрный экран, NV12 не согласуется).
- НЕ просить у грабера 720×576 и НЕ ставить io-mode=4 — Arkmicro только 640×480 MJPG, io-mode=4 ломает дешёвые UVC.
- НЕ запускать/доверять старому /opt без редеплоя (истина — репо/install.sh).
- pyproject.toml — чужое незакоммиченное изменение, не трогать.
- docs/baseline/ — снимок отката, не редактировать «под текущее».

## ПРАВИЛА РАБОТЫ
- §0 аудит/план → diff → НЕ коммить без ОК (не «allow all»), поэтапно. Push — только после ОК на diff.
- Дозируй: один логический блок, жди подтверждения. Без простыней.
- Помечай блоки «На u1:» / «На u2:» / «В PowerShell:».
- ЯВНО говорить, когда включать ДРОН (только финал живой камеры, B1). Монитор на u1. Русский.
