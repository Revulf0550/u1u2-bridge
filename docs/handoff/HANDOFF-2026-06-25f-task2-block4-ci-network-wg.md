# HANDOFF — 2026-06-25 (f) · Задача 2 блок 4: CI-оснастка + сеть + WireGuard-трекинг аудированы (по живому)

> Преемник: `HANDOFF-2026-06-25e-task2-block3-systemd-planB.md` (закоммичен в начале этой
> сессии как `6d1eba9`, запушен).
> **Принцип: факт = подтверждён живой командой в ЭТОЙ сессии.** Память / компакт-саммари /
> «помню» / приложенные к чату документы / `/mnt/project/` — допущение, помечать или проверять.
> Весь фактаж ниже — из live-чтения файлов репо: выгрузка `cp <file> ~/Downloads/` → перетаскивание
> в чат → `view`, плюс `md5sum`/`grep` по выгруженным копиям на стороне Claude. НЕ из памяти.
>
> Метки источников:
> `[RAW]` — прочитано целиком/диапазонами через `view` в ЭТОЙ сессии (первичный факт).
> `[RAW-grep]` — в выгруженном grep-выводе, открытом/отфильтрованном через `view`/`bash`.
> `[RAW-md5]` — целостность копии подтверждена `md5sum` (копия = рабочее дерево).
> `[AGENT]` — только саммари агента Claude Code, сырьём НЕ видел → пере-сверить.
> `[MEM]` — память / прошлые хэндоффы / прошлые чаты.
> `[PI-TODO]` — требует подтверждения на живой Pi (Pi-чеклист §5, НЕ закрыто).

---

## §0 — Состояние репозитория (на момент написания 25f)

- **На старте сессии** HEAD был `6bad9f0` (25d), синхрон `[RAW]` (`git log --oneline -5` + `status -sb`).
  Единственный untracked — сам 25e-хэндофф (ожидаемо).
- **В начале сессии** 25e застейджен **точечно** (`git add <file>`, НЕ `-A`), `diff --cached --stat`
  = 1 файл / +243 `[RAW]`, закоммичен `6d1eba9`, запушен `6bad9f0..6d1eba9 main -> main` `[RAW]`.
- **После этого — read-only:** 0 коммитов, 0 мутаций до конца сессии. Весь блок 4 — чтение.
- На момент написания этого файла HEAD = **`6d1eba9`**, дерево чистое, синхрон `[RAW]`.
- Серия git-аудита Задачи 2: `399c883` (блок 1) → `6bad9f0` (блок 2) → `6d1eba9` (блок 3).
  Этот файл (25f) = блок 4, ляжет следующим scoped-коммитом.
- Следствие: дерево чистое → **рабочая копия == HEAD `6d1eba9`** → выгруженные `cp`-копии = HEAD.
  **Следующий чат всё равно ре-верифицирует §0 как всегда.**

---

## §1 — Что закрыто в этой сессии (блок 4: CI + сеть + WG)

Задача 2 = полный сквозной аудит стека → итог в `docs/roadmap/`. Блок 1 (CRSF+видео)=25c,
блок 2 (install/udev/smoke)=25d, блок 3 (systemd+planB)=25e. **Эта сессия: блок 4 — CI-оснастка,
сетевые секции install.sh, WireGuard-трекинг + закрыта перепроверка §2b (overlay).**

### Прочитано СЫРЬЁМ за сессию
| Файл / секция | Метод | Вердикт |
|---|---|---|
| `Makefile` (30) | `[RAW]` view целиком | Чист. Новая **P3-CI-divergence** (нет shellcheck-цели). |
| `verify.ps1` (59) | `[RAW]` view целиком | Сильный гейтинг. Подтверждает дивергенцию (5 стадий вкл. shellcheck). |
| `format.ps1` (7) | `[RAW]` view целиком | Тривиален, ≡ Makefile `format`. Без находок. |
| `install.sh §2b` (111–158) | `[RAW]` view + `[RAW-md5]` `38f68409…` | **Перепроверка overlay ЗАКРЫТА.** Стр.135 = m1 (см. P1-overlay). |
| `install.sh §3` (159–168) | `[RAW]` view | Авто-iface здрав (UP-фильтр + override). Без находок. |
| `install.sh §4` (170–204) | `[RAW]` view | Netplan корректен. Подтверждён **P3 #3** (`netplan apply\|\|true`). |
| `install.sh §7c` (290–305) | `[RAW]` view | UFW аддитивный, сильный. Новая **P3-ufw-asymmetry**. |
| `install.sh TRANSPORT` (56, 230–242, 278–287) | `[RAW-grep]` | Единый источник истины. Новая **P3-transport-default**. |
| WG-трекинг (`ls-files`/`grep` clean) | `[RAW-grep]` (отфильтровано через `bash` по выгрузке) | Только шаблон tracked; реальные конфиги gitignored. **P2-smoke-wg подтверждён сырьём.** |

> `install.sh` целиком на диске (`~/Downloads/install.sh`, 19823 B, `md5 38f684099e51ad7390f59a0fe10b59c3`
> = HEAD `6d1eba9`). Последний коммит, тронувший install.sh — **`7def5d8` (2026-06-13)** `[RAW]`
> «docs: примирить UART7 m2/pin26 как боевой деплой». Все коммиты после 06-13 трогали только `docs/`.

### Метод (держать)
`cp <file> ~/Downloads/` → перетащить → `view`. Свёрнутый stdout Claude Code (`+N lines`) = саммари
агента = **ДОПУЩЕНИЕ**. Для grep с большими паттернами — выгружать в файл и фильтровать самому
(`git grep` тащит SVG/lock-шум: clean-греп раздулся до 1.46 MB из-за `docs/diagrams/*.svg`, пришлось
исключать `*.svg`/`uv.lock` и резать через `bash` на стороне Claude). `view` усекает середину >16 KB.

---

## §2 — Реестр находок Задачи 2 (накопительный, обновлён блоком 4)

> Аудит read-only: всё **каталогизировано, НЕ исправлено**. Правки = отдельное scoped-решение
> ПОСЛЕ всех блоков. P1-overlay / setup_udev / любые правки u2/UART7 — за drone-safety-гейтом.

### P1-overlay — install.sh §2b деплоит m1 вместо боевого m2 — **ПОДТВЕРЖДЁН+УСИЛЕН СЫРЬЁМ. Перепроверка ЗАКРЫТА.**
**Сырьём этой сессии:**
- `install.sh:135` `[RAW]`: `U_BOOT_FDT_OVERLAYS="device-tree/rockchip/overlay/rk3588-uart7-m1.dtbo"`
  — код деплоя регистрирует **m1**. Коммент `:132` («pins 29/38 (TX/RX) via overlay m1») с кодом
  согласован → §2b **внутренне непротиворечив** (оба m1).
- Раскол — **между §2b (m1) и §7-комментом `:267–268`** `[RAW]`: «ttyS7 = UART7 overlay **m2** →
  физ. pin 26 (GPIO1_B5), TX управления. Проверено gpio readall 2026-06-13. RX = m1 (pin 29/38)
  неактивен → трек D». Плюс футер `:417` `[RAW]` «UART7 на пинах **29/38** → REBOOT» (тоже m1).
- **Источник раскола `[RAW]`:** последний коммит install.sh — `7def5d8` (2026-06-13) «примирить
  m2/pin26 как боевой». Он поправил §7-коммент + CLAUDE.md на m2, **а §2b-код (стр.135) оставил
  на m1**. Классическая частичная правка (коммент ≠ код), от которой предостерегают уроки проекта.
- **Следствие:** свежий `install.sh u2` зарегистрирует m1 → после reboot UART7 встанет на пины
  **29/38**, НЕ на боевой **pin 26**. Живая Pi работает на m2 только потому, что её донастроили руками
  в bringup, а не чистым install.sh → **install.sh НЕ воспроизводит полевую конфигурацию.** Реальный
  деплой-баг.
- **Правка (для блока правок, за drone-safety-гейтом):** `install.sh:135` `m1→m2`, примирить
  коммент `:132` и футер `:417`. **Предусловие** `[PI-TODO]` §5: подтвердить наличие
  `rk3588-uart7-m2.dtbo` в `/lib/firmware/.../overlay/` — не чинить вслепую.

### P1-udev — `setup_udev.sh` осиротел (RS485→CH340/UART7) — **БЕЗ ИЗМЕНЕНИЙ (25d)**
Не трогалось в этой сессии. Статус и `[PI-TODO]` прежние.

### P2-smoke-wg — smoke_test пингует мёртвую 10.10.0.x — **ПОДТВЕРЖДЁН СЫРЬЁМ (был [MEM] из 25d).**
**Сырьём этой сессии `[RAW-grep]`:**
- `smoke_test.sh:26` `PEER_IP_WG="10.10.0.2"`, `:31` `PEER_IP_WG="10.10.0.1"` — подсеть **10.10.0.x**.
- Весь остальной деплой-стек — **10.8.0.x**: `install.sh:232`, `u2/video_tx.sh:30`,
  `switch-mode.ps1:57–58`, `bench/crsf_udp_source.py:14/15/74`, `hardware/README.md:21`.
- Логика `smoke_test.sh:121–130` `[RAW-grep]`: `if ip link show wg0` → ping `$PEER_IP_WG`; не прошёл
  → **FAIL**; нет wg0 → `warn`-skip (норма для bench). На живом Режиме 1 wg0 поднят на **10.8.0.x** →
  ping 10.10.0.x не пройдёт → **ложный FAIL** постдеплойного smoke-теста.
- **Причина:** 10.10.0.x — проектный overlay-WG (Этап 2, не развёрнут); smoke писался под план
  адресации, а не под bench-факт 10.8.0.x.
- **Правка (блок правок):** `smoke_test.sh:26/31` → 10.8.0.x (или параметризовать из env/TRANSPORT).
  Не за drone-гейтом (только диагностика), но scoped-решением после всех блоков.

### P2-planB — недостают 3 модуля в install.sh §5 — **ЗАКРЫТ КАК НАХОДКА (25e), без изменений.**
`channel_map.py` + `crsf_telemetry.py` + `telemetry_logger.py` не копируются install.sh §5 → краш-луп
`joystick-to-crsf` на drone-u1. Детали в 25e §2. В этой сессии не трогалось.

### P3 — наблюдения, не блокеры (накопительно)
- **#1–8** — из 25c–25e (RKMPP-гейт по MODE; авто-iface; `netplan apply||true`; smoke проверяет
  энкодер на u1; `MODE` не в шапке опц-env; #6 закрыт; #7 js0/evdev рассинхрон; #8 незамапл.=992).
- **#3 `netplan apply||true` — ПОДТВЕРЖДЁН сырьём.** `install.sh:200` `[RAW]`: упавший netplan не
  оборвёт install (`set -e` обойдён через `|| true`). Минор.
- **P3-CI-divergence (новое) `[RAW]`.** `make verify` = `lint typecheck test` (Makefile:9), scope
  `common tests` — **НЕ запускает shellcheck**. `verify.ps1` = те же 4 + shellcheck по всем `.sh`
  рекурсивно (стадия 5/5, стр.36–56). Гейты **не эквивалентны**: `make verify` строго слабее, не
  ловит битый bash (`install.sh`/`video_*.sh`/`setup_udev.sh`). Правка: добить Makefile shellcheck-целью
  ИЛИ задокументировать `verify.ps1` как канон-гейт.
- **P3-static-scope (новое) `[RAW]`.** ruff/mypy в обоих входах бьют только `common tests`
  (`Makefile:17/20–21`, `pyproject.toml files/src`). `bench/`+`hardware/` Python **вне статика**.
  Вероятно by design (диагностика), сверить мотивацию на bench/hardware-блоке.
- **P3-ufw-asymmetry (новое) `[RAW]`.** `install.sh §7c` добавляет u1 только видео-allow (5600 от
  u2, стр.301), но НЕ CRSF-allow (14552). Если ufw на u1 когда-нибудь включат — телеметрия-возврат
  u2→u1:14552/udp молча дропнется (тот failure mode из уроков 2026-05-24/06-05). Латентно: дизайн
  заложил u1-ufw неактивным, потому пока безвредно. Низкий приоритет.
- **P3-transport-default (новое) `[RAW]`.** `install.sh:56` `TRANSPORT="${TRANSPORT:-tunnel}"` —
  дефолт **tunnel**. `:231–234` tunnel→10.8.0.x, direct→192.168.1.x (единый источник истины, чисто).
  Боевой режим — №2 (direct). Свежий `./install.sh u2` без `TRANSPORT=direct` уведёт CRSF в неживой
  туннель 10.8.0.x. Механизм здравый — мисматчит только дефолт vs поле. Известно (CLAUDE.md 06-05).
  Низкий приоритет (операторский caveat, не дефект кода).

### Гейт (не P-находка) — evtest-калибровка channels.default.toml — без изменений (25e §2).

---

## §3 — Подтверждённая сильная инженерия (перенос знания, сырьём этой сессии)

- **`verify.ps1` `[RAW]`:** честный fail-fast гейтинг нативных exe через `$LASTEXITCODE -ne 0 → exit 1`
  после каждой стадии (`$ErrorActionPreference="Continue"` корректен для uv/shellcheck); shellcheck с
  PATH-проверкой + `winget`-подсказкой (не молчаливый skip), рекурсивно по всем `.sh`, `.venv` исключён,
  один вызов сплатом `& shellcheck @scripts`; самоописание шапки совпадает с телом (5 стадий).
- **`format.ps1` ≡ `Makefile` `format` `[RAW]`:** `ruff format` → `ruff check --fix`, scope `common
  tests` — эквивалентны, дивергирует только `verify`.
- **`install.sh` TRANSPORT `[RAW/RAW-grep]`:** `TRANSPORT` — единый источник истины (стр.56, 230–234);
  env CRSF (`crsf-*.env`) и видео (`video.env` PEER_HOST, стр.285) деривятся из него для ОБОИХ режимов;
  `CRSF_PORT=14552` (236) канон; `PEER_IP_OVERRIDE` только для netplan (181–184), на env CRSF не влияет;
  в tunnel netplan авто-скип (`SKIP_NETPLAN=1`, стр.63–65). video_tx.sh fallback 10.8.0.6 — только
  аварийный (install.sh:282).
- **`install.sh §7c` UFW `[RAW]`:** строго аддитивно — `ufw allow` идемпотентно, **никогда** `ufw
  enable`, никогда смена default-policy; на inactive-стороне правила спят; привязка к выстраданным
  урокам 2026-05-24/06-05 (UFW молча дропал новый source).
- **`install.sh §3` авто-iface `[RAW]`:** берёт первый не-`lo` интерфейс **в UP** (стр.162) — отсекает
  поднятые-но-неподключённые wlan/usb-eth; ручной `IFACE=`-override + явная ошибка с примером.
- **WG-секреты `[RAW-grep]`:** реальные `wg0.conf`+ключи gitignored (`.gitignore:16/25`), tracked только
  шаблон `docs/wg-template.conf`; приватных ключей в tracked нет. Согласовано с disaster-recovery:
  `/etc/wireguard/` невосстановим из репо → отдельный Уровень-1 бэкап с каждой Pi обязателен.

---

## §4 — Очередь аудита (осталось, всё по живому репо)

Из §4 25e минус закрытое (CI + сеть + WG):
- **install.sh §1 / §8 / §9 / §10 / §11** — зависимости (§1, стр.78–100), udev-вызов адаптеров (§8,
  306–338), cage/wayland подготовка u1 (§9, 339–359), sysctl UDP-буферы (§10, 360–367), запуск (§11,
  368–413). Частично `[MEM 25d]`, сырьём в этой сессии НЕ читаны.
- **Тестовая оснастка:** `bench/` (`crsf_udp_source.py`, `loopback.py`), `hardware/`
  (`crsf_jitter_test.py`, `crsf_smoke_test.py`, `README.md`). Здесь же закрыть мотивацию
  **P3-static-scope** (почему bench/hardware вне ruff/mypy).
- **`smoke_test.sh` целиком** — в этой сессии видел только строки 26/31/121–130 `[RAW-grep]`; прочитать
  целиком сырьём (там же может всплыть P3 #4 «smoke проверяет энкодер на u1»).
- `tests/unit/` (9 модулей) — на усмотрение.
- **Стале-доки `docs/HANDOFF.md`, `docs/DEPLOYMENT.md`** — пометить «исторические» отдельным решением.
  Подтверждено сырьём: DEPLOYMENT описывает overlay-WG 10.10.0.x (проектный Этап 3), живой стенд —
  bench-WG 10.8.0.x; DEPLOYMENT в этой части опережает факт.

---

## §5 — Pi-чеклист (один транскрипт, Режим 2 WG off, ПОД конкретный список) — обновлён

В этой сессии на живую Pi НЕ ходили. Список:
- **P1-overlay (приоритет):** `cat /boot/extlinux/extlinux.conf`; `grep U_BOOT_FDT_OVERLAYS
  /etc/default/u-boot`; **`ls /lib/firmware/$(uname -r)/device-tree/rockchip/overlay/ | grep uart7`**
  → какой overlay активен И **существует ли `rk3588-uart7-m2.dtbo`** (предусловие правки install.sh:135).
- **P1-udev:** `ls -l /etc/udev/rules.d/90-u1u2-uart.rules` (ожидаемо: нет файла).
- **P2-smoke-wg (новое в чеклист):** на живом узле подтвердить подсеть wg0 — `ip -br addr show wg0`
  (ожид. **10.8.0.x**, НЕ 10.10.0.x) → это валидирует, что правка smoke_test 10.10→10.8 верна.
- **P3 #7 (js0/evdev):** `ls /dev/input/` (есть ли `js0` и `event0`), `evtest` → подтвердить, что
  `DEVICE` в `joystick.env` указывает на реальный evdev-узел (это же — evtest-гейт калибровки).
- **Из 25c §4:** `lsusb` + `v4l2-ctl --list-formats` (Arkmicro 640×480 MJPG); `ip -br link` (имя iface,
  ожид. `enP3p49s0`); `systemctl is-active` 6 сервисов; развёрнутые `/etc/u1u2-bridge/*.env`; рантайм
  `udp_drop`, RSSI/LQ, clock-skew (~52 мин на u2).

---

## §6 — Рабочие инварианты (перенос из 25e, не менять)

- Язык — русский. SSH всегда `ssh -i ~/.ssh/u1u2 ubuntu@<ip>`.
- **Drone-safety gate:** пропеллеры сняты + Boxer off перед любым рестартом CRSF-сервисов; явное
  разрешение оператора на каждое питание дрона.
- **Два канала ARDOR:** Claude Code (WG on, репо/git/SSH по туннелю) ⟂ батч-PowerShell-транскрипт
  (WG off, мост `192.168.1.x`). Kill-switch `AllowedIPs=0.0.0.0/0` — параллельного доступа нет.
  Переключение инструментов — анонсировать.
- Серийный ping (`-n 4`+/`-Count 2`+) за радио обязателен — одиночный врёт на холодном ARP.
- §0-аудит (git + live-сервисы) ПЕРЕД любыми мутациями/хэндоффом. Транспорт — по `PEER=` в env,
  НЕ по `systemctl is-active`.
- Scoped-коммиты: НИКОГДА `git add -A`; `diff --cached --stat` перед каждым; форвард-онли.
  (В этой сессии так и коммитили 25e-хэндофф: `6bad9f0..6d1eba9`, 1 файл +243.)
- Файлы хэндоффа/скриптов — генерировать на стороне Claude, отдавать скачиванием, не heredoc.
- Старый 8-жильный кабель из проекта вычищен — НЕ упоминать как контекст.
- **Урок stdout:** Claude Code сворачивает многострочный stdout; `git grep` тащит SVG/lock-шум
  (раздулся до 1.46 MB). Для аудита — выгружать в файл, фильтровать самому (`bash`/`view`), НЕ
  полагаться на саммари. `view` усекает середину — читать диапазонами, проверять полноту.
- **Агентские реплики «тот же файл, что выгружали ранее» — НЕТОЧНЫ.** В этой сессии агент мислейбелил
  известный P2-smoke-wg как «новую находку 1» — сверять каждое утверждение агента сырьём.
- **Вложения чата и `/mnt/project/` — СТАЛЕ.** Аудит ТОЛЬКО по живому репо (подтверждено: `/mnt/project`
  install.sh — «Orange Pi 5», живой — «Orange Pi 5 / 5 Max»).

---

## §7 — Немедленный TODO на старте следующего чата

1. **Режим 1 (WG on).** `cd C:\Users\ARDOR\Documents\Projects\u1u2-bridge;
   git --no-pager log --oneline -5; git status -sb` — §0-аудит. Ожидаемо **HEAD = хэш коммита 25f**
   (этот файл), синхрон, чисто (ПРОВЕРИТЬ, не полагаться). Если статус свернётся — выгрузить в файл.
2. **Продолжить Задачу 2: install.sh §1/§8–§11 сырьём** (`~/Downloads/install.sh` уже на диске =
   HEAD; читать диапазонами через `view`). Затем **bench/ + hardware/** (закрыть P3-static-scope),
   **smoke_test.sh целиком**, опц. `tests/unit/`.
3. Стале-доки `docs/HANDOFF.md`/`docs/DEPLOYMENT.md` — пометить «исторические» отдельным решением.
4. Живая Pi — только под список §5, одним батчем (WG off → `Start-Transcript` → WG on → файл).
5. Когда все блоки закрыты — **сводный итог Задачи 2 → `docs/roadmap/`** (scoped-коммит): реестр §2
   + приоритизация правок (P1-overlay / P1-udev / P2-smoke-wg / P2-planB + P3-список:
   CI-divergence / static-scope / ufw-asymmetry / transport-default / #1–8).
6. **Задача 3** (A-to-Z инструкция сборки) — ПОСЛЕ Задачи 2 и устаканивания железа; обязана включить
   evtest-калибровку channels.toml (гейт §2 25e).

---

## Приложение — границы знания (честные оговорки)

- **md5-сверка:** для `install.sh` (`38f684099e51ad7390f59a0fe10b59c3`) копия сверена `[RAW-md5]` =
  HEAD `6d1eba9`. Для `Makefile`/`verify.ps1`/`format.ps1` md5 НЕ сверялся; привязка к `6d1eba9`
  держится на чистом §0 (дерево чистое → рабочая копия = HEAD). Содержимое прочитано целиком.
- **Код НЕ исполнялся** в этой сессии: ни `verify.ps1`/`make verify`/`ruff`/`mypy`/`pytest`/`shellcheck`,
  ни рантайм. Все вердикты — статическое ревью. P2-smoke-wg (ложный FAIL) выведен из чтения логики
  121–130 + сверки подсетей, не из запуска smoke_test.
- **Security не аудирован** (наследуется): открытый UDP без фильтрации source в `crsf_bridge`/
  `joystick_to_crsf`; P3-ufw-asymmetry латентен. Отдельная тема.
- **Полнота списка tracked-файлов = `[AGENT]`** (`wg-lsfiles.txt` в чат не долетел; что WG-файл ровно
  один — `docs/wg-template.conf` — подтверждено `[RAW-grep]` по паттерну, но полный ls-files сырьём не
  читан). При финальной сборке итога — добить.
- **Живая Pi не затрагивалась** — все `[PI-TODO]` открыты.
