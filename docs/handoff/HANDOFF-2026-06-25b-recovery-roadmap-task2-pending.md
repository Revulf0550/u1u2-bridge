# HANDOFF — 2026-06-25 (b) · Disaster-recovery → roadmap; Задача 2 НЕ начата

> Преемник: `docs/handoff/HANDOFF-2026-06-25-trackB-roadmap-done.md` (коммит `ab9854c`).
> Принцип: **факт = подтверждён живой командой в этой сессии.** Память / компакт-саммари /
> «помню» / приложенные к чату документы — допущение, помечать unconfirmed или проверять.
> Весь фактаж ниже — из live-команд сессии 2026-06-25 (Claude Code Режим 1 / WG on +
> PowerShell-админ на ARDOR), НЕ из памяти и НЕ из вложений чата.

---

## §0 — Состояние репозитория (аудит факта, конец сессии)

- HEAD `6239d68`, `## main...origin/main` — синхрон, без ahead/behind. Дерево чистое.
- Подтверждено `git commit` + `git push` (`ab9854c..6239d68 main -> main`) + `git status -sb`
  в конце сессии.
- **Git-хвостов нет.**

### Трассировка
- Старт сессии: HEAD `ab9854c` (подтверждён §0-аудитом вживую), синхрон, дерево чистое.
- +1 коммит за сессию: `6239d68` (disaster-recovery.md, см. §1.C).

---

## §1 — Что сделано в этой сессии

### A. §0-аудит вживую
`git log`/`git status` в Claude Code. HEAD `ab9854c` подтверждён. Гипотеза прошлого
хэндоффа (`ab9854c` = коммит самого хэндоффа поверх `f7144ef`) — подтвердилась.

### B. Развязка «транспорт узлов vs доступ ARDOR» — подтверждена ЖИВЫМИ командами
Ранее эти факты были допущениями из памяти. Теперь сняты командой:
- **`wg show` (PowerShell-админ, ARDOR):** интерфейс `NSU-pc`; peer endpoint
  `95.140.147.108:51820`; **`allowed ips: 0.0.0.0/0, ::/0`** (kill-switch ПОДТВЕРЖДЁН
  живой командой — прежде был «из памяти»); handshake свежий (~1–2 мин); 2.14 GiB rx.
- **`ping -n 4 10.8.0.7`** (туннельный u2): 0% потерь, среднее **~210 мс**.
- **`ping -n 4 192.168.1.10`** (прямой u2): 100% потерь — ОЖИДАЕМО при WG on (kill-switch
  `0.0.0.0/0` заворачивает весь трафик ПК в туннель; прямого пути к 192.168.1.x с ПК нет).
- **Вывод (важно для образа):** RTT ~210 мс = трафик к Pi реально идёт через публичный
  endpoint наружу и обратно, НЕ локальным хопом, хотя ARDOR+u1+u2 на одном свитче End-A
  (подтв. пользователем). Для полного образа диска туннельный путь непригоден.

### C. Цель «образ диска» → стратегия recovery, закоммичена
Пользовательская цель уточнена: **не побитовая копия, а воспроизводимость** —
«залить на новый носитель рабочую систему и быстро вернуть в строй».
Создан `docs/roadmap/disaster-recovery.md` (73 строки, house-style трека):
- **Уровень 1 (сейчас):** бэкап только уникального состояния (wg0.conf+ключи — приватный
  ключ невосстановим; живые crsf-*.env; udev с серийниками; netplan). Восстановление:
  чистая ОС → `git clone` → `install.sh` → накат бэкапа. Не требует WG off.
- **Уровень 2 (ЗАВЕРШАЮЩИЙ ЭТАП проекта):** полный `dd`-клон «воткнул и летит» — по
  прямому пути WG off (гигабит по свитчу), НЕ через туннель. Делать, когда железо
  устаканено (грабер cx231xx + трек D определён).
- **Бэклог (QoL):** сужение `AllowedIPs` (исключить 192.168.1.0/24) для параллельного
  прямого+туннельного доступа. Предусловие выполнено (ARDOR на LAN). Ослабляет kill-switch.
Коммит: `6239d68`, запушен.

### D. ОШИБКА Слоя-1 поймана и исправлена (метод-урок)
В начале чата я выдал «Слой 1» аудита (CRSF-тракт) и заявил находку **[P1]: порт 14552 в
install.sh отсутствует, там 14550/14551**. Это было **НЕВЕРНО.**
- Греп по ЖИВОМУ репо (`git grep 14552`): `install.sh:236 CRSF_PORT=14552`,
  `install.sh:298 ufw ... port 14552`, baseline-env `docs/baseline/u1/crsf-p1.env:3` и
  `u2/crsf-elrs.env:3` → `LISTEN=0.0.0.0:14552`, плюс `CLAUDE.md:143`. Порты 14550/14551 в
  живых `*.env`/`*.sh`/`install.sh` НЕ найдены — вычищены в stageB (14550/14551→14552).
- **Причина ошибки:** я аудировал по приложенному к чату `install.sh` (там 14550/14551),
  а он СТАЛЕ относительно живого репо. Нарушен фундаментальный принцип «факт = живая
  команда». Греп исправил до коммита — ложная находка в roadmap НЕ попала.

---

## §2 — Следующий шаг: Задача 2 (НЕ начата)

Очередь §6 хэндоффа 06-24: **1 → 2 → 3**. Задача 1 (трек B) закрыта в `f7144ef`.
Эта сессия была **внеплановой вставкой** (recovery-стратегия) перед Задачей 2.

**Задача 2 — полный сквозной аудит стека → `docs/roadmap/`.** НЕ НАЧАТА.
- «Слой 1», выданный в этом чате, **АННУЛИРОВАН** (строился на стале-вложении, см. §1.D).
  Аудит начинать с нуля.
- Обзор: `crsf_bridge.py`, видео-пайплайны (`video_tx.sh`/`video_rx.sh`), `install.sh`,
  systemd-юниты, сеть, железо + приоритизация улучшений.
- **НЕ чистый синтез:** хэндофф 06-24 требует свериться с парой ЖИВЫХ конфигов для
  авторитетности. Возможен выход в Режим 2 (WG off) под конкретные проверки — решать
  в новом чате под конкретные конфиги, не вхолостую.
- Большой объём → свежий чат с полным контекстным бюджетом (этот выполнен).

**Задача 3 — A-to-Z инструкция сборки.** ПОСЛЕДНЕЙ, после устаканивания железа.

---

## §3 — КРИТИЧНО для нового чата: вложения ≠ живой репо

> Главный урок этой сессии. Не повторить ошибку §1.D.

- **Приложенные к чату документы и файлы в `/mnt/project/` — СТАЛЕ.** Конкретно
  приложенный `install.sh` показывает порты 14550/14551 и udev для CP2102N — в живом репо
  давно 14552 и другое состояние. Приложенный `HANDOFF.md` — старый generic (CP2102N,
  MS2130, «не тестировано на железе», 8-жильный кабель) — противоречит факту (полевые
  тесты пройдены, кабель из проекта вычищен).
- **Задачу 2 вести ТОЛЬКО через `git grep`/`cat` по живому репо в Claude Code.** Не
  цитировать вложения как факт. Если вложение и живой репо расходятся — прав живой репо.
- Это прямое следствие фундаментального принципа: факт = живая команда, не снимок.

---

## §4 — Рабочие инварианты (перенос, не менять)

- Язык — русский.
- SSH всегда `ssh -i ~/.ssh/u1u2 ubuntu@<ip>`.
- Drone-safety gate: пропеллеры сняты + Boxer off перед любым рестартом CRSF-сервисов;
  явное разрешение оператора на каждое питание дрона.
- Два канала ARDOR: Claude Code (WG on, репо/git/SSH по туннелю) vs батч-PowerShell-
  транскрипт (WG off, доступ к мосту 192.168.1.x). Переключение инструментов — анонсировать.
- **Kill-switch `AllowedIPs=0.0.0.0/0` — ПОДТВЕРЖДЁН живым `wg show` 2026-06-25.** WG on на
  ПК → прямого доступа к 192.168.1.x нет, всё в туннель (RTT к u2 ~210 мс). WG off → прямой
  путь по свитчу End-A.
- Серийный ping (`-n 4`+) обязателен за радио — одиночный врёт на холодном ARP.
- §0-аудит (git + live-сервисы) ПЕРЕД любыми мутациями/хэндоффом. Транспорт — по `PEER=`
  в env, НЕ по `systemctl is-active`.
- Scoped-коммиты: НИКОГДА `git add -A`; diff-review (`git diff --cached`) перед каждым
  коммитом; форвард-онли.
- Файлы хэндоффа/скриптов — генерировать на стороне Claude, отдавать скачиванием, не
  heredoc. Claude Code копирует из Downloads в репо.
- Старый 8-жильный кабель из проекта вычищен намеренно — НЕ упоминать как контекст.

---

## §5 — Немедленный TODO на старте следующего чата

1. **Режим 1 (WG on).** `git --no-pager log --oneline -5 && git status -sb` — §0-аудит факта.
   Ожидаемо: HEAD `6239d68`, синхрон с origin, дерево чистое (ПРОВЕРИТЬ, не полагаться).
2. **Запустить Задачу 2** (полный сквозной аудит стека → `docs/roadmap/`), С НУЛЯ.
   - Вести по живому репо: `git grep`/`cat`, НЕ по вложениям чата (см. §3).
   - Перед live-сверкой конфигов — решить, какие именно нужны и нужен ли выход в Режим 2.
     Не уходить в WG off вхолостую.
   - Может ссылаться на `docs/roadmap/track-b-video-analog.md` (трек B, итог Задачи 1).
