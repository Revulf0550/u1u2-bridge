# HANDOFF — 2026-06-26 (b) · Блок P3 закрыт (4 коммита) + 3 docs/build из P2-обёртки

> Преемник: `HANDOFF-2026-06-26a-p2-block-pushed.md` (старт-HEAD `e9c109d`).
> **Принцип: факт = подтверждён живой командой в ЭТОЙ сессии.** Память / компакт-саммари /
> приложенные к чату документы / `/mnt/project/` — допущение, помечать или проверять.
> Метки: `[RAW]` сырьё · `[RAW-md5]` md5-сверка · `[MEM]` память/прошлые хэндоффы · `[PI-TODO]` нужна живая Pi.
>
> Реестр находок — `docs/roadmap/task2-stack-audit.md` (теперь синхронен с фактом, см. §1).
> Этот файл — рабочая обёртка: старт, что сделано, что осталось, инварианты.

---

## §0 — Состояние репозитория (подтверждено сырьём на конец сессии)

- **HEAD = `0a888f3`**. `[RAW]` (git log -10 + status -sb на конец сессии)
- Дерево чистое, `## main...origin/main` — синхрон с origin, всё запушено. `[RAW]`
- Серия этой сессии (7 коммитов) поверх старт-HEAD `e9c109d`:
  `9cc4efa` → `e524309` → `35a37af` → `2b7e365` → `951f7ef` → `0a888f3`.
  (плюс старт-точка прошлой сессии `e9c109d` уже была запушена.)

**Старт нового чата:** `cd C:\Users\ARDOR\Documents\Projects\u1u2-bridge;
git --no-pager log --oneline -8; git status -sb` — §0-аудит. Ожидаемо HEAD = `0a888f3`,
синхрон, чисто — **ПРОВЕРИТЬ, не полагаться на хэш из этого файла**.

---

## §1 — Что сделано в этой сессии (Режим 1, WG on, рабочее железо НЕ трогалось)

Семь коммитов. Каждый: сырьё (часто md5-сверенная копия в Downloads → view) → diff на review →
точечный str_replace по содержимому → стейдж по имени → пауза на `--cached --stat` → коммит
(многострочные сообщения через `printf … > /tmp/commitmsg.txt; git commit -F` — обход обрыва heredoc) → push.

1. **P2-deployment-stale** — `9cc4efa` (баннер) + `e524309` (отметка в реестре).
   - `docs/DEPLOYMENT.md` был активным runbook без дисклеймера. Добавлен баннер
     **«ЧАСТИЧНО УСТАРЕЛ»**: §4 (`install.sh`) — валидный канон, устарели ручные env/udev-шаги §5/§6.
   - ВАЖНО по ходу выяснено сырьём: реестровое «насквозь стале» НЕТОЧНО — док **гибрид**.
     Это уточнение внесено в реестр (`e524309`, строка СТАТУС в записи P2-deployment-stale).
   - 5 осей расхождения тела с каноном (в баннере): инстансы `@tx1/tx2`→`@p1/@elrs`;
     порт `14550/14551`→`14552`; `/dev/ttyACM-*`+`setup_udev.sh` (RS485-эра); WG `10.10.0.x`→`10.8.0.x`;
     «Pi 5»→«5 Max» (`enP3p49s0`). Тело НЕ переписано — backlog (отдельная задача).

2. **P3 CI-divergence** — `35a37af`.
   - `make verify` (Makefile:9 `verify: lint typecheck test`) не имел shellcheck-стадии, тогда как
     `verify.ps1` имеет `[5/5] shellcheck` по всем .sh. Добавлены: цель `shellcheck:` (явный список
     5 файлов: install.sh setup_udev.sh smoke_test.sh u1/video_rx.sh u2/video_tx.sh), `.PHONY`, +в `verify:`.
   - Выбран ЯВНЫЙ список (не `$(shell find)`) — репо стабильное, предсказуемость важнее авто-подхвата.
   - ⚠ Коммит БЕЗ локального прогона: `make`/shellcheck — Linux-side, на Windows канон `verify.ps1`.
     Синтаксис тривиален, TAB-рецепт подтверждён `cat -A` (`^I`). [PI-TODO: при случае прогнать `make verify` на Pi.]

3. **P3 bench-doc-stale** — `2b7e365` (hardware) + `951f7ef` (bench). Две части — две оси.
   - Часть 1: scp-примеры в `hardware/{crsf_jitter_test.py, crsf_smoke_test.py, README.md}` несли
     старый home-LAN IP `192.168.31.100` → заменены на плейсхолдер `<u2-pi-ip>` (не дрейфует при смене
     транспорта; WG-вариант `10.8.0.7` в README оставлен живым).
   - Часть 2: `bench/crsf_udp_source.py` — порт `14550`→`14552` в 2 докстринг-примерах + help `--peer`.
     Только порт; адрес `10.8.0.7` не тронут; логика sweep/arm-toggle (drone-safety AUX1-low) не затронута.
     `--peer required`, дефолта нет — порт был только в тексте, не в рантайме.

4. **P3-таблица к факту** — `0a888f3`.
   - 2× отметка РЕШЕНО (CI-divergence→`35a37af`, bench-doc-stale→`2b7e365`+`951f7ef`).
   - 2× переклассификация в **by-design** (подтверждено сырьём `install.sh`):
     - **ufw-asymmetry**: каждый узел открывает порт того, что ПРИНИМАЕТ (u2←CRSF 14552, u1←видео 5600).
       u1 CRSF-allow не нужен до обратной телеметрии (трек D). НЕ дефект.
     - **transport-default**: дефолт `tunnel` осознан, защищён авто-`SKIP_NETPLAN` (install.sh:63) +
       объяснён в шапке. НЕ дефект.

---

## §2 — Подтверждено сырьём install.sh в этой сессии (md5-сверенная копия, view)

Прочитан `install.sh` целиком (HEAD 0a888f3). Зафиксировано `[RAW]`:
- **`CRSF_PORT=14552`** (строка 239) — канон порта подтверждён ГЛАЗАМИ (раньше был только пересказ/реестр).
- **P1-overlay РЕАЛЬНА и подтверждена:** строки 132-135 пишут `rk3588-uart7-m1.dtbo` (pins 29/38),
  а коммент §7 (строки 270-271) говорит, что боевой путь — **m2, pin 26** (проверено gpio readall 2026-06-13),
  m1 неактивен (трек D / RX-телеметрия). Это та самая P1-overlay — деплоит overlay не того пути.
- **MODE/TRANSPORT/ROLE-матрица** (строки 50-66, 226-291, 371-392): u1 bench=`crsf-bridge@p1`,
  u1 drone=`joystick-to-crsf` (plan B, USB-HID, ttyUSB0/event0), u2=`crsf-bridge@elrs` (ttyS7).
  Peer из TRANSPORT (tunnel: u1=10.8.0.6/u2=10.8.0.7; direct: 192.168.1.x).
- udev: install.sh §8 (строки 309-332) — только напоминание, правила пишет `setup_udev.sh` (P1-udev).

---

## §3 — Что осталось

**Не за гейтом (можно в Режиме 1, следующий заход):**
- **#4** (минор, спорный) — `smoke_test.sh` проверяет `mpph264enc` (энкодер) на роли u1 (только декод).
  Реестр: «работает (один пакет gstreamer1.0-rockchip1 включает enc+dec), сем. неточно». СЫРЬЁМ НЕ
  РАЗОБРАН в этой сессии. Перед правкой свериться: install.sh:102-109 гейтит RKMPP-чек только для
  `MODE==bench` — возможно, и в smoke это by-design. Решить по сырью smoke_test.sh.
- **DEPLOYMENT.md тело** — переписать на канон (backlog; баннер уже прикрывает). Большая правка, отдельная сессия.

**За drone-safety-гейтом (нужен [PI-TODO], отдельная Pi-сессия, Режим 2 WG off):**
- **P1-overlay** — install.sh:135 деплоит m1.dtbo (pins 29/38) вместо боевого m2 (pin 26).
  Предусловие правки: подтвердить на Pi существование `rk3588-uart7-m2.dtbo`
  (`ls /lib/firmware/$(uname -r)/device-tree/rockchip/overlay/ | grep uart7`). БЕЗ него не чинить вслепую.
- **P1-udev** — `setup_udev.sh` не вызывается из install.sh + (кандидат) `setup_udev.sh:190` печатает
  устаревшую restart-команду `crsf-bridge@tx1 crsf-bridge@tx2` (боевые: `@p1`/`@elrs`). За тем же гейтом.
- **#7** — js0/evdev рассинхрон (`DEVICE` в joystick.env vs реальный узел), evtest-гейт. [PI-TODO]

**Задача 3** (A-to-Z сборка) — после устаканивания железа; обязана включить evtest-калибровку
`channels.default.toml`.

---

## §4 — Pi-чеклист (Режим 2, WG off, один транскрипт; [PI-TODO], НЕ закрыт)

- **P1-overlay (приоритет):** `cat /etc/default/u-boot | grep -A2 UART7`; `ls /lib/firmware/$(uname -r)/
  device-tree/rockchip/overlay/ | grep uart7` → существует ли `rk3588-uart7-m2.dtbo`. `gpio readall` (pin 26 vs 29/38).
- **P1-udev:** `grep -n 'crsf-bridge@tx' setup_udev.sh` (ожид. устаревшая строка ~190);
  `ls -l /etc/udev/rules.d/90-u1u2-uart.rules`.
- **CI-divergence verify:** при случае `make verify` на Pi (проверить, что shellcheck-цель реально работает).
- **#4:** `cat smoke_test.sh` секция RKMPP — гейтится ли по MODE/ROLE.
- **Рантайм:** `lsusb`; `v4l2-ctl --list-formats` (Arkmicro 640×480 MJPG); `ip -br link` (ожид. `enP3p49s0`);
  транспорт — по `PEER=` в env, НЕ по `systemctl is-active`.

---

## §5 — Рабочие инварианты (не менять)

- Язык — русский. SSH всегда `ssh -i ~/.ssh/u1u2 ubuntu@<ip>`.
- **Два канала ARDOR:** Claude Code (WG on, Режим 1, репо/git/SSH) ⟂ батч-PowerShell (WG off, Режим 2,
  мост `192.168.1.x`). Kill-switch `AllowedIPs=0.0.0.0/0` — параллельного доступа нет. Переключение анонсировать.
- **Drone-safety gate:** винты сняты + Boxer off перед любым рестартом CRSF-сервисов.
- §0-аудит (git + при Pi-работе live) ПЕРЕД любыми мутациями/хэндоффом. Транспорт — по `PEER=` в env.
- Scoped-коммиты: НИКОГДА `git add -A`; `diff --cached --stat` перед каждым; форвард-онли.
- **Ритм правки:** сырьё → diff на review пользователю → точечный str_replace по содержимому (НЕ по
  номерам строк) → `git diff` → стейдж по имени → пауза на `--cached --stat` → коммит → пауза → push с «ок».
  Один логический блок за раз.
- **Многострочные commit-сообщения — через `printf … > /tmp/commitmsg.txt; git commit -F /tmp/...`**, НЕ
  heredoc в одну вставку (heredoc обрывался при копировании — терял закрывающий разделитель, висел).
- **Сырьё > саммари агента.** Агент в этой сессии: путал номера строк, подменял команды (cat→sed на другой
  файл), сворачивал вывод в `+N lines`, сам перепроектировал правки и пушил «применять». Целевые файлы
  читать **md5-сверенной копией в Downloads → drag → view**, НЕ через stdout агента (сворачивается).
- Вложения чата и `/mnt/project/` — СТАЛЕ. Работа — ТОЛЬКО по живому репо.
- Commit-сообщения самодостаточны: без путевых ссылок «см. §N / :NNN» в теле.

---

## §6 — Немедленный TODO на старте нового чата

1. **Режим 1 (WG on).** §0-аудит: `git --no-pager log --oneline -8; git status -sb`.
   Ожидаемо HEAD = `0a888f3`, синхрон, чисто (ПРОВЕРИТЬ).
2. Ветка:
   - **#4** (Режим 1, лёгкое, но СНАЧАЛА свериться сырьём smoke_test.sh — может оказаться by-design) — добить или закрыть by-design;
   - затем **Pi-сессия (Режим 2, WG off, drone-safety-гейт)** для P1-overlay / P1-udev / #7 — по §4 чеклисту.
3. Любую правку — прежним ритмом (§5). P1 + рестарты CRSF — только за drone-safety-гейтом.
