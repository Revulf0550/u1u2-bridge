# HANDOFF → ПОЛЕВЫЕ ТЕСТЫ 1–3 ВЫПОЛНЕНЫ · система в боевом Режиме №2 (verified) · СЛЕДУЮЩЕЕ = инструкция «от А до Я» · u1u2-bridge · 2026-06-05

> Точка входа в новый чат. **Полевой план Шаги 1–3 ВЫПОЛНЕНЫ live с дроном:** Режим №2 (радио) и Режим №1 (tunnel)
> оба доказаны на железе (связь/управление/ARM/живое видео); `switch-mode.ps1` переключает в обе стороны live;
> power-cycle обеих Pi пережит. Система **возвращена в боевой Режим №2 и проверена** (CRSF симметричный ~24 KB/s `drop=0`, живое видео с дроном).
> **СЛЕДУЮЩЕЕ (приоритет нового чата по решению пользователя):** трек ДОКУМЕНТАЦИЯ — в первую очередь **инструкция «от А до Я»** (с нуля до рабочей системы + графические схемы всех узлов). Критичного/срочного к правке нет.
> §0: любая работа — после сверки с фактом (репо + железо), не по памяти.
> Этот файл СУПЕРСЕДИТ `HANDOFF-2026-06-05-stageD-done-pre-field-drone.md`.

---

## 0. СНАЧАЛА ПРОЧИТАЙ (в репо)
- `docs/PLAN.md` — **канонический мастер-план** (концепция двух режимов, Этапы A–E). Авторитетный источник по Этапу E и текущим отметкам — свериться с ним фактически.
- `docs/baseline/` — снимок отката рабочей CRSF-конфигурации. **НЕ редактировать** (tx-имена там намеренные).
- `CLAUDE.md` — Architecture + Lessons & Incidents + roadmap (вкл. **Пульт-джойстик №2**). NB: ~94.5 KB > лимита 40 KB — долг по trim.
- Репо: GitHub `Revulf0550/u1u2-bridge`, локально `C:\Users\ARDOR\Documents\Projects\u1u2-bridge`. На Pi код в `/opt/u1u2-bridge`, env в `/etc/u1u2-bridge`.
- **§0:** любая работа — после сверки с фактом (репо + железо), не по памяти.

---

## ⚠️ ДОСТУП (WG-состояния ВЗАИМОИСКЛЮЧАЮЩИЕ)

ARDOR ходит в интернет / к ассистенту / в Claude Code **через WireGuard**:
- **WG ВКЛЮЧЁН** → интернет + ассистент + Claude Code есть, **НО** kill-switch (`AllowedIPs 0.0.0.0/0`) режет мост `192.168.1.x`.
- **WG ВЫКЛЮЧЕН** → мост `192.168.1.x` доступен (SSH к Pi), **НО** нет интернета → ассистент/Claude Code не работают.

**Паттерн для моста = БАТЧ:** WG-off → команды в обычном PowerShell с `Start-Transcript` → WG-on → прислать транскрипт. **Claude Code — только для репо/git при WG-on.**

SSH: `ssh -i ~/.ssh/u1u2 ubuntu@192.168.1.20` (u1) / `ubuntu@192.168.1.10` (u2), sudo NOPASSWD. (В Режиме №1 / дома — Pi по `10.8.0.6` / `10.8.0.7` при WG-on.)

---

## СОСТОЯНИЕ (факт, live-verified 2026-06-05 ~15:30)

**Система в боевом Режиме №2 (direct, радио), проверена с дроном.**

- **Физика:** обе Pi на радио-концах (u1 у оператора, u2 на дальнем конце), на мосту CPE710. ARDOR на мосту (USB-NIC `Ethernet 3` `192.168.1.50`). WG-OFF — боевое положение.
- **git:** HEAD `51bfa84`. За сессию коммитов НЕ было (`switch-mode.ps1` деплоит через `git archive HEAD`, репо не меняет). `main...origin`=0/0. Незакоммичено: `M pyproject.toml` (чужое) + untracked (PNG, прошлые хэндоффы, `handoff-2026-05-25/`, **этот файл** → положить в `docs/handoff/`).
- **u1 (`u1-pi`, `192.168.1.20`):** `enP3p49s0 192.168.1.20/24` (+ DHCP `192.168.31.x` дома), `wg0 10.8.0.6`. `crsf-bridge@p1`+`video-rx`+`wg-quick@wg0` = `active`. `crsf-p1.env`: `/dev/ttyUSB0` (CH340, пульт Boxer), 420000, LISTEN `:14552`, PEER `192.168.1.10:14552`.
- **u2 (`u2-pi`, `192.168.1.10`):** `enP3p49s0 192.168.1.10/24` (+ DHCP дома), `wg0 10.8.0.7`. `crsf-bridge@elrs`+`video-tx`+`wg-quick@wg0` = `active`. `crsf-elrs.env`: `/dev/ttyS7` (UART7 → ELRS-TX), PEER `192.168.1.20:14552`. `video.env` PEER_HOST `192.168.1.20`.
- **CRSF (live):** u1 `uart->udp ~24 KB/s` ↔ u2 `udp->uart ~24 KB/s`, `udp_drop=0`, симметрично. Критерий = симметрия + `drop=0` (темп зависит от пакетрейта Boxer, видели 22–31 KB/s). Обратный telemetry-поток = 0 (см. B-telem).
- **Видео (live, с дроном):** u2 `video_tx.sh` (грабер Arkmicro 640×480 MJPG, `mpph264enc cbr bps=2500000 gop=15 profile=66`) → UDP `192.168.1.20:5600` → радио → u1 `video_rx.sh` (`cage`+`waylandsink`, `JITTER_LATENCY=50`). Живое движение на u1 — есть.
- **netplan location-adaptive (live-подтверждён):** `enP3p49s0` несёт И статику `192.168.1.x`, И DHCP `192.168.31.x` (`50-cloud-init.yaml` + `99-u1u2-bridge.yaml`). Switcher netplan НЕ трогает.

---

## ✅ ЧТО ДОКАЗАНО ЭТОЙ СЕССИЕЙ (2026-06-05)
1. **Шаг 1 — Режим №2 + дрон:** связь, управление, ARM, живое видео.
2. **Шаг 2 — `switch-mode.ps1 tunnel` + дрон:** §7b → `video.env 10.8.0.6`, CRSF-peer → `10.8.0.x`, управление+ARM+живое видео через туннель (RTT ~140 мс).
3. **Шаг 3 — power-cycle обеих Pi (дома, tunnel):** обе встали в tunnel (env пережил), `wg0` авто, 4 сервиса авто-`active`, CRSF симметричный `drop=0`. Headless u2 загрузилась штатно.
4. **Возврат `switch-mode.ps1 direct`:** §7b → `video.env 192.168.1.20`, CRSF-peer → `192.168.1.x`, UFW идемпотентен, 4 `active`, Режим №2 проверен над радио.

---

## 🗺️ МАСТЕР-ПЛАН — статус (✅ done · 🔄 частично/отложено · ❌ не сделано · ❓ сверить с PLAN.md)

> Сведено из прошлых чатов + live-фактов сессии. Авторитет — `docs/PLAN.md`.

**Концепция:** беспроводная замена 8-жильного кабеля У1↔У2; **Режим №1** (tunnel/WG = база отката) и **Режим №2** (direct/радио CPE710 = боевой).

**Этапы:**
- ✅ **A — чистка + автозапуск:** A0 baseline-снимок (`docs/baseline/`), A1–A5 (генерация CRSF-схемы p1/elrs в install.sh), видео под systemd (cage+waylandsink от root), чистка доков. 4 юнита active+enabled.
- ✅ **B — проверка Режима №1 (через WG):** B1 видео e2e с живым сигналом ✅; B-telem ✅ (обратная телеметрия = 0, НЕ-блокер, в backlog — полётные данные идут через OSD видео-канала; «0» лишает только RSSI/LQ-индикации и аудио-предупреждений на пульте; failsafe независим; гипотеза причины: не разведён обратный TLM-провод ELRS → RX `/dev/ttyS7` либо конфиг модуля (half-duplex single-wire CRSF) — **НЕ код**, бридж симметричен); B2/B3 CRSF+reboot ✅; 🔄 B1-latency сознательно отложена с WG на полевой замер.
- ✅ **C — Режим №2 codified + cutover на радио (CPE710):** Mode 2 зашит в install.sh, cutover выполнен.
- ✅ **D — `switch-mode.ps1` + фикс `video.env`:** переключатель в обе стороны, §7b пишет video-peer в обоих режимах.
- 🔄 **E** — в `docs/PLAN.md` заложен как «спросить пользователя, что дальше» (**открыт по дизайну**, не предопределён). **Пользователь ответил 2026-06-05: Этап E = трек ДОКУМЕНТАЦИЯ И ОБРАЗЫ** (см. ниже). Полевые тесты на дистанции и production hardening — это BACKLOG, НЕ часть E.

**Полевые тесты (эта сессия):** ✅ Шаг 1 (Режим №2+дрон) · ✅ Шаг 2 (tunnel+дрон) · ✅ Шаг 3 (power-cycle) · ✅ возврат+проверка Режима №2.

**🆕 ТРЕК ДОКУМЕНТАЦИЯ И ОБРАЗЫ (новый, приоритетный):**
- ❌ 📘 **Инструкция «от А до Я»** — с нуля (новая Pi → установка ОС Joshua Riek Ubuntu 24.04 → сеть/статика → CPE710 → WireGuard → распайка/подключение всех узлов → сервисы → проверка) до рабочей проверенной системы, чтобы человек собрал без труда. + **графические схемы всех узлов** (что куда припаяно/соединено/подключено). **← ПРИОРИТЕТ №1 в новом чате.**
  - *Готовые активы (переиспользовать):* `boxer_jr_bay_adapter.html` + 4 SVG-модуля (Boxer JR-bay → SN74HC14N инвертор → CH340G → u1 `/dev/ttyUSB0`) — это аппаратная часть входа CRSF на u1.
  - *Нарисовать заново:* u2-сторона (UART7 `/dev/ttyS7` → ELRS-TX), грабер Arkmicro → VRX (composite), подключение CPE710 (AP/Client, Ethernet), питание Pi (USB-C 5V), общая топология.
  - *Safety в инструкцию:* прозвонка мультиметром **ДО подачи питания** (риск спалить UART на u1 при ошибке монтажа); в JR-bay pin VBAT ~7.8 В — не подать на сигнальную линию.
- ❌ 📸 **Полные образы u1 и u2** (бэкап-клоны). Рекомендация по порядку: снять РАНО (система сейчас в доказанно-рабочем состоянии; особенно ценно для headless u2 — образ = быстрый возврат при смерти носителя).
- ❌ 📄 **Инструкция переключения Режим 1↔2** (по `switch-mode.ps1` + физика + WG) в стиле cheatsheet.
- 🎨 **Стандарт оформления** для всей документации — стиль `u1u2-bridge-cheatsheet.html` (см. ниже).

**❌/❓ BACKLOG / ДАЛЬШЕ (всё держим, прорабатываем детально):**
- ❌ Тюнинг `JITTER_LATENCY` под радио (сейчас =50, тюнилось под WG 180 мс; радио RTT ~2–8 мс → можно опускать; мерить на ЖИВОМ движущемся видео — CBR маскирует no-signal).
- ❌ Полётные испытания на дистанции 500–1000 м: латенция видео gopher-методом, RSSI/потери на пульте, throughput, стабильность WG handshake.
- ❌ **Пульт-джойстик №2 (USB-HID)** — отложенный план B, «обязательно применим позже»: любой USB-геймпад вместо Boxer (`joystick_to_udp.py` на u1 → `joystick_to_crsf.py` на u2 → ELRS UART7); +10 мс, без пайки, без Lua/telemetry. Детали в roadmap CLAUDE.md.
- ❓ **CTRL-канал** (переключение каналов VRX с У1 — 2-й RS485 исходного кабеля). Сейчас мост обслуживает только канал управления дроном. Проверить, нужен ли / реализован — свериться с PLAN.md и уточнить у пользователя.
- ❌ **Production hardening:** hardware watchdog (`rockchip-wdt`; systemd `RuntimeWatchdogSec=20`/`RebootWatchdogSec=2min`), read-only rootfs (overlayroot), логи на tmpfs, OSD-оверлей RSSI/SNR на видео.
- ❓ **Гигиена секретов:** ротация WireGuard-ключей (засветились в dev-чатах) — проверить, сделана ли.
- ❌ **Trim `CLAUDE.md`** (~94.5 KB → <40 KB; старые bringup-уроки 2026-05-18/22 в архивный doc).
- ❌ **Cleanup на Pi:** `~/deploy.tar` + `~/u1u2-deploy` (switcher пересоздаёт `/home/ubuntu/u1u2-deploy`; рабочий код в `/opt`).
- ❌ **Косметика `install.sh` echo:** строки `(peer будет 192.168.1.x)` и футер `Проверка ... (direct)` зашиты под direct, не отражают tunnel. Конфиг при этом верен.

**✅ РЕШЁННЫЕ ВОПРОСЫ (НЕ открывать заново):**
- Грабер = **Arkmicro, только 640×480 MJPG** (не MS2130 как предполагалось; без `io-mode=4`, не просить 720×576).
- Сетевой интерфейс Pi = **`enP3p49s0`** (не `end0`; install.sh всё равно автоопределяет).
- **RS485 auto-direction на 420k — снят с повестки:** архитектура ушла на CH340 (u1) + аппаратный UART7 (u2) напрямую, RS485 в тракте нет.
- **WG-over-CPE710 (UDP/51820) — неактуально:** Режим №2 = direct без WG в тракте; WG только для Режима №1 (через интернет).
- Дисплей u1 — **`cage`+`waylandsink`** (kmssink даёт VOP2 timeout); `videoconvert` обязателен (NV12).

---

## ШАГ 0 для нового чата — §0-сверка (БАТЧ, PowerShell, WG-OFF; ТОЛЬКО ЧТЕНИЕ)

> Система в Режиме №2 на мосту → `192.168.1.x`, WG-off. (Если работа в новом чате — чисто документация без железа, ШАГ 0 можно отложить; но любая правка железа/конфига — после сверки.)

```powershell
Start-Transcript -Path $HOME\Desktop\s0-audit.txt -Force
$REPO="$HOME\Documents\Projects\u1u2-bridge"; $K="$HOME\.ssh\u1u2"
git -C $REPO --no-pager log --oneline -5; git -C $REPO status -sb
ping -n 4 192.168.1.20
ping -n 4 192.168.1.10
ssh -i $K -o ConnectTimeout=8 ubuntu@192.168.1.20 'hostname; ip -4 -br addr; for u in crsf-bridge@p1 video-rx wg-quick@wg0; do echo ==$u==; systemctl is-active $u; done; echo --crsf--; journalctl -u crsf-bridge@p1 -n 2 --no-pager; echo --env--; cat /etc/u1u2-bridge/crsf-p1.env'
ssh -i $K -o ConnectTimeout=8 ubuntu@192.168.1.10 'hostname; ip -4 -br addr; for u in crsf-bridge@elrs video-tx wg-quick@wg0; do echo ==$u==; systemctl is-active $u; done; echo --crsf--; journalctl -u crsf-bridge@elrs -n 2 --no-pager; echo --env--; cat /etc/u1u2-bridge/crsf-elrs.env /etc/u1u2-bridge/video.env'
Stop-Transcript
```
**Ожидаю:** HEAD `51bfa84`; ping обе 0%; 4 сервиса `active`; CRSF симметричный `drop=0` (если Boxer вкл); env как в СОСТОЯНИИ.

---

## 🎨 СТАНДАРТ ОФОРМЛЕНИЯ ДОКУМЕНТАЦИИ (по решению пользователя)

Эталон — `u1u2-bridge-cheatsheet.html` (**приложить в новый чат для 1:1 повторения**). Стиль:
- **HTML field-manual:** шрифты IBM Plex Sans / IBM Plex Mono; тёплая «paper» палитра (`--bg #faf9f5` …) + авто dark-mode (`prefers-color-scheme: dark`, `--bg #14140f` …). Печатается прилично в PDF.
- **SVG-схемы:** явный тёмный фон `<rect fill="#1a1a18">` (видны при любой теме клиента). Палитра «один цвет = один сигнал»: зелёный вход `#3B6D11` (блок `#27500A`), коралл выход `#D85A30` (`#712B13`), синий VCC `#185FA5` (`#0C447C`), GND провода `#444441` / шина `#2C2C2A` / подписи amber `#BA7517`, чип тело `#2C2C2A` обводка `#888780` текст `#F1EFE8`. Каждый модуль отдельно + общая цепь.
- Контент пользователь проверяет и корректирует.
- Перед генерацией HTML — читать `/mnt/skills/public/frontend-design/SKILL.md`.

Существующие активы для переиспользования (из чата «CPE710 физическая настройка», 2026-05-26): `boxer_jr_bay_adapter.html` + 4 SVG-модуля (Boxer JR-bay, SN74HC14N, CH340G, общая цепь) — основа аппаратной части инструкции «от А до Я».

---

## 📦 `switch-mode.ps1` (корень репо)
- `tunnel`: Pi **дома**, WG **ВКЛ** (к Pi по `10.8.0.6/.7`). `direct`: Pi **на мосту**, WG **ВЫКЛ** (по `192.168.1.20/.10`).
- Делает: preflight-ping → `git archive HEAD` → `scp` → `install.sh TRANSPORT=<mode> SKIP_APT=1 SKIP_NETPLAN=1 MODE=bench` на обе → рестарт `crsf-bridge@*`+`video-*` по `[y/N]` → `is-active` + хвост CRSF. §7b пишет `video.env PEER_HOST=$CRSF_PEER` в ОБОИХ режимах.
- Запуск из **обычного PowerShell**; при WG-off (direct) оборачивать в `Start-Transcript`. `-NoRestart` — деплой без рестарта. Скачан заново → `Unblock-File`. netplan не трогает. Физику/WG скрипт НЕ делает.

## 🔙 ОТКАТ В РЕЖИМ №1
`switch-mode.ps1 tunnel`: Pi домой → WG ВКЛ → `switch-mode.ps1 tunnel` → подтвердить рестарт. Режим №1 — известно-рабочая база, UFW-direct-правила аддитивны.

---

## ❗ НЕ ДЕЛАЙ
- **ДРОН включать / рестартить CRSF при armed — ТОЛЬКО спросив пользователя ЯВНО** (рестарт `@elrs`/`@p1` рвёт управление ~2 с).
- **НЕ запускать `install.sh` вручную без `SKIP_NETPLAN=1`** (перепишет netplan, риск lockout) и **без `SKIP_APT=1` на мосту** (`apt` упадёт без интернета). netplan не трогать.
- **WG на ARDOR держать ВЫКЛ при работе с мостом** (kill-switch). Вкл — для Режима №1 / git / запуска switcher'а в `tunnel`.
- **НЕ запускать клонский (`a8573d6`) `install.sh` из `~/u1u2-bridge` на Pi** — деплоить только HEAD (switcher через `git archive`).
- НЕ `kmssink` (VOP2 timeout) — только `cage`+`waylandsink`; НЕ убирать `videoconvert` (чёрный на NV12). Грабер — 640×480 MJPG, без `io-mode=4`.
- `pyproject.toml` — чужое, не трогать. `docs/baseline/` — снимок, не трогать.
- В Claude Code Bash-тулзе НЕ `cd ~/...` (резолвится в Windows-домашку). Длинные файлы — через файл-артефакт, не вставкой в терминал.

---

## ТОПОЛОГИЯ / ДОСТУП
- u1 = RX + монитор, `192.168.1.20` (мост) / `10.8.0.6` (WG); u2 = TX (**headless**), `192.168.1.10` / `10.8.0.7`. Ключ `~/.ssh/u1u2`, sudo NOPASSWD.
- ARDOR (хост ПК; в шапке PowerShell-транскрипта машина `REVULF`, профиль `C:\Users\ARDOR` — ПК переименован, на работу не влияет): встроенный `Ethernet` `192.168.31.150` (дом, интернет через WG); USB-NIC `Ethernet 3` `192.168.1.50` (мост, без gateway). WG `NSU-pc` `10.8.0.5`.
- CPE-AP `192.168.1.2`, CPE-Client `192.168.1.3` (раскладка: AP у u1, Client у u2); Pharos UI с отдельного ПК `192.168.0.254`.
- В Режиме №2 Pi на ПРОТИВОПОЛОЖНЫХ концах радио. В Режиме №1 — обе в домашнем свитче.

## ПРАВИЛА РАБОТЫ
- §0 аудит/план → diff → **НЕ коммить/менять без ОК**, поэтапно.
- Дозируй: один логический блок, жди подтверждения. Не вываливать длинные списки/код, если можно разбить.
- В начале шага — карта подключений (где Pi, WG on/off, что трогаем). Помечай «На u1:» / «На u2:» / «В PowerShell:» / «В Claude Code:».
- Claude Code — для локальных команд/git при WG-on; батч-PowerShell — для моста при WG-off. Переключаться не молча.
- При хэндоффе/передаче — сначала полный аудит факта (репо + железо + прошлые чаты), потом резюме. Не по памяти.
- Монитор на u1. Язык — русский.

---

## УРОКИ ЭТОЙ СЕССИИ (кандидаты в CLAUDE.md — внести при тримминге, не дублировать уроки Этапа D)
1. **Power-cycle resilience доказан live:** обе Pi (вкл. headless u2) встают в последнем задеплоенном режиме (env на диске — единственный источник, авто-детекта нет), `wg-quick@wg0` enabled → `wg0` поднимается всегда, 4 сервиса авто-`active`. Самый безопасный момент для power-cycle-теста — когда Pi дома.
2. **`switch-mode.ps1` проверен в ОБЕ стороны live** (tunnel↔direct) неразрушающе; §7b пишет `video.env` корректно в обоих транспортах.
3. **Косметика `install.sh`:** info-echo не отражают tunnel-режим — не путать echo с реальным состоянием (сверять рантайм-логом/env-файлом).

---

## ▶️ СТАРТ НОВОГО ЧАТА
1. Загрузить: **этот хэндофф** + **`u1u2-bridge-cheatsheet.html`** (эталон оформления) + по возможности SVG-модули/`boxer_jr_bay_adapter.html`.
2. Первая задача: **инструкция «от А до Я»** (+ графические схемы узлов) в стиле cheatsheet. Образы u1/u2 — рано в этом же треке.
3. Перед любой правкой железа/конфига — ШАГ 0 (§0-сверка). Чистая документация — можно без него.
